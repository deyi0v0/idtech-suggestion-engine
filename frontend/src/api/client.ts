// TODO: Export these async functions (all using fetch to /api/...):
// - sendChatMessage(chatRequest) → POST /api/chat
// - getComparison(merchantInput) → POST /api/compare
// - checkCompatibility(productIds) → POST /api/compatibility
// - downloadPDF(merchantInput) → POST /api/pdf, return blob
