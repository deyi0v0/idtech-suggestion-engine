# TODO: Implement check_compatibility(product_ids: list[int], db: Session) -> tuple[bool, list[str]]:
#  handles the cases where user make a choice of software + hardware, p1 feature
#  can also used as a second check of the return pair of bundle
#  a tool of product_filtering.py