# TODO: Create the FastAPI app instance.
# Include all four routers (chat, compare, compatibility, pdf) with an /api prefix.
# Add CORS middleware allowing the frontend origin (http://localhost:5173).
# On startup, call create_tables() from db/database.py.
# Use dotenv to load the .env file.
