# TODO: Implement build_recommendation_prompt(merchant: MerchantInput, products: list[dict]) -> str:
#   Build a prompt that includes the merchant's requirements and the filtered product list,
#   asking the LLM to explain why each product is a good fit.
#
# TODO: Implement build_chat_prompt(message: str, history: list[dict], context: str = "") -> str:
#   Build a conversational prompt with chat history.
