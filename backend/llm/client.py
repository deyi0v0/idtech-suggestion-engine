# TODO: Implement async def get_completion(prompt: str, context: str = "") -> str:
#   Use the openai Python package.
#   Load API key from environment.
#   Call client.chat.completions.create with model gpt-4o-mini.
#   Return the response content string.
#   Wrap in try/except and return a fallback error message.
